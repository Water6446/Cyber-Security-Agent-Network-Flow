# dataset — large, public, re-downloadable from cicresearch.ca (lives in data/)
GeneratedLabelledFlows/
MachineLearningCSV/
*.csv
*.zip
*.pcap

# MITRE ATT&CK STIX bundle — downloaded/cached by threat_intel.py into data/
enterprise-attack.json

# agent run artifacts — findings/mappings reports + generated diagrams
outputs/

# python
__pycache__/
*.pyc
.venv/

# secrets — never commit API keys
.env

# local session files
cowork_handoff.md
